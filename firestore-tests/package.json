{
  "name": "finertia-firestore-tests",
  "private": true,
  "type": "module",
  "description": "Security rules tests. Kept out of frontend/ so the app bundle never sees these dependencies.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project finertia-rules-test 'node rules.test.mjs'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
