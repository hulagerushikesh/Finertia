# Pinned to the versions Cloud Run resolved on 2026-09-13 (revision
# finertia-api-00003-lzf). The test suite passes on these exactly; an
# unpinned `pandas` silently jumped 2.3 -> 3.0 between deploys, which is
# the kind of surprise a backtest engine should not get in production.
# Bump deliberately: change the pin, run the suite, redeploy.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
python-dotenv==1.2.3
yfinance==1.7.0
pandas==3.0.5
numpy==2.4.6
python-multipart==0.0.32
firebase-admin==7.5.0
